# Anthropic API key — NEVER commit a real key
ANTHROPIC_API_KEY=sk-ant-...

# JWT secret — generate with: openssl rand -hex 32
JWT_SECRET=your-32-byte-hex-secret-here

# Per-session call budget (default 20 — covers a full Dream→Build→Check→Show loop with headroom)
SESSION_CALL_BUDGET=20
